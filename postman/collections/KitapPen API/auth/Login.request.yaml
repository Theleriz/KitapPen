$kind: http-request
method: POST
url: '{{baseUrl}}/auth/login/'
order: 2000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "username": "demo_user",
      "password": "password123"
    }
auth:
  type: noauth
