$kind: http-request
method: POST
url: '{{baseUrl}}/auth/refresh/'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "refresh": "{{refreshToken}}"
    }
auth:
  type: noauth
