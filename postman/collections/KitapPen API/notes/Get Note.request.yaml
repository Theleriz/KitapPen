$kind: http-request
method: GET
url: '{{baseUrl}}/notes/:noteId/'
order: 4000
headers:
  - key: Authorization
    value: 'Bearer {{accessToken}}'
pathVariables:
  - key: noteId
    value: '{{noteId}}'
